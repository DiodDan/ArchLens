package io.archlens.runtime;

import io.quarkus.runtime.annotations.Recorder;

/**
 * ArchLens runtime recorder.
 * ArchLens is a purely build-time / dev-mode extension.
 */
@Recorder
public class ArchLensRecorder {
    // Intentionally empty — ArchLens is dev-only and has no runtime behaviour.
}
